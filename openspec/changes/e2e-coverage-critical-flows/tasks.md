# Tasks: e2e-coverage-critical-flows

> Generated by sdd-tasks. Each task = one atomic commit per `work-unit-commits` skill.
> Total: **12 tasks across 4 slices** (4 stacked-to-main PRs).
> Strict TDD: page objects + helpers FIRST (T0.1, T0.2), then RED specs (T1.1, T2.2, T2.3, T3.2, T3.3), then app-code `data-testid` patches (T1.1, T2.1, T3.1 — fused with their consuming specs in T1.1, separated in T2.x/T3.x per design §7).
> Chain strategy: **stacked-to-main** (sticky from `gga-hook-diff-only` cycle, engram #198).
> Total estimated diff: **+1,876 / -112 changed lines** across the 12 tasks.

---

## Slice 0 — Test infrastructure refactor (PR 1)

> Goal: build the test-infrastructure layer (helpers + page objects + config) so the 5 spec files in Slices 1-3 can use it. Zero new tests in this slice — pure plumbing + config.

### T0.1 — `refactor(tests): extract tests/helpers.ts + delete broken security-helpers loginAsAdmin`

> ✅ **DONE** — commit `3fce4bd` — 8 files, +196/-128. 6 inline copies consolidated into `tests/helpers.ts:loginAsAdmin` with the rich `gym-config.spec.ts:97-111` hydration guard. Broken `tests/utils/security-helpers.ts:loginAsAdmin` (wrong creds) deleted.

- **Files**:
  - **New**: `tests/helpers.ts` (~120 LOC)
  - **Modified** (inline `loginAsAdmin` → import from `./helpers`):
    - `tests/gym-config.spec.ts` (line 97: 15 LOC removed → 1 import added)
    - `tests/admin-e2e.spec.ts` (line 8)
    - `tests/promocion-e2e.spec.ts` (line 17)
    - `tests/security-admin.spec.ts` (line 65)
    - `tests/dnd-rutina.spec.ts` (line 6)
    - `tests/cache-invalidation.spec.ts` (line 240)
  - **Modified**: `tests/utils/security-helpers.ts` — delete broken `loginAsAdmin` function (lines 27-50, uses `12345678/admin123` which does NOT match the seed). Keep ALL other exports: `createTestUser`, `setExpiredCookie`, `clearSession`, `getRoutineIds`, `apiRequest`, the `test` fixture, `loginAsUser`. (~20 LOC removed)
- **Body**: Create `tests/helpers.ts` with `loginAsAdmin(page)` that uses the SEEDED credentials `11111111`/`nando123` AND waits for the `Panel de Administración` heading (matches the rich pattern in `gym-config.spec.ts:97-111` — guards against the GGA-FOLLOWUP-4 hydration race). Also export `cleanTestData(page)`, `waitForToast(page, message)`, `waitForServerAction(page, actionPath)`, and a re-export of `setExpiredCookie` for the future `auth.spec.ts`. Replace all 6 inline `loginAsAdmin` definitions with `import { loginAsAdmin } from './helpers'`. Delete the broken (wrong-creds) `loginAsAdmin` from `tests/utils/security-helpers.ts` (the function is never imported — the 6 inline copies use the correct seed creds directly).
- **Acceptance**:
  - `grep -r "async function loginAsAdmin" tests/` returns **0 matches** (only the export in `tests/helpers.ts`).
  - `grep -r "function loginAsAdmin" tests/utils/security-helpers.ts` returns 0 matches (broken helper deleted).
  - All 6 refactored specs still pass green: `pnpm test --grep-invert @slow` runs without diff.
  - The 5.2.3 isolation spec stays green (this commit does NOT change isolation patterns).
  - No new TypeScript errors (`pnpm tsc --noEmit`).
- **Depends on**: none
- **TDD**: **Refactor** — consolidates 6 duplicates; existing tests must stay green (no behavioral change).
- **Diff size**: **+130 / -110** (≈240 changed lines)
- **Special notes**:
  - The new `loginAsAdmin` MUST match the `gym-config.spec.ts:97-111` pattern (15s timeout for the admin layout heading), not the simpler `security-helpers.ts:35` pattern. This is the GGA-FOLLOWUP-4 mitigation.
  - The 6 spec files have varied inline definitions; some use `input[type="password"]`, others use `input[id="password"]`. The new helper must use the form-label-first approach (`getByLabel('Contraseña')`) per the Playwright skill's selector priority. All 6 must continue to pass.
  - `loginAsNonAdmin` in `security-admin.spec.ts:47` stays untouched (uses different signature).
  - `createTestUser` from `security-helpers.ts` is NOT deleted (used by `security-admin.spec.ts`).

### T0.2 — `feat(tests): add base-page + 6 page object skeletons`

> ✅ **DONE** — commit `5ecabfe` — 7 files, +756/-0. BasePage + AuthPage + 5 admin page objects created. Selector priority: getByRole > getByLabel > getByText > getByTestId. Page objects are thin wrappers (no cross-page nav, no auth flows, no data cleanup, no direct API calls) per design Decision 6.

- **Files** (all NEW, 7 files in 1 commit per design §7):
  - `tests/pages/base-page.ts` (~40 LOC) — `BasePage` abstract class with `waitForToast`, `waitForNetworkIdle`, `waitForAdminLayout` (sentinel for hydration via `Panel de Administración` heading)
  - `tests/pages/AuthPage.ts` (~50 LOC) — login form interactions: `goto()`, `loginAsAdmin()`, `fillDni()`, `fillPassword()`, `submit()`, `expectError()`
  - `tests/pages/RoutineAdminPage.ts` (~120 LOC) — `/admin/rutinas`, `/admin/rutinas/new`, `/admin/rutinas/[id]`: `goto()`, `gotoNew()`, `fillNombre()`, `fillDescripcion()`, `selectTipo()`, `submitCreate()`, `expectCreated()`, `deleteByName()`, `expectInList()`
  - `tests/pages/FeriadoAdminPage.ts` (~100 LOC) — `/admin/feriados`: `goto()`, `fillFecha()`, `toggleTodoDia()`, `fillHoraInicio()`, `fillHoraFin()`, `submitCreate()`, `expectCreated()`, `expectDuplicateError()`, `deleteByFecha()`, `expectNotInList()`
  - `tests/pages/PromocionAdminPage.ts` (~100 LOC) — `/admin/promociones`
  - `tests/pages/DescuentoAdminPage.ts` (~80 LOC) — `/admin/descuentos-duracion`
  - `tests/pages/TrainerAdminPage.ts` (~100 LOC) — `/admin/trainers`
- **Body**: Create the 7 new page-object files. Each page object extends `BasePage` and is a **thin wrapper** (Decision 6: no cross-page navigation, no auth flows, no data cleanup, no direct API calls). Per-page methods follow the role/label-first selector strategy (Decision 4): `getByRole` → `getByLabel` → `getByText` → `getByTestId` fallback. Some methods are **skeletons** at this commit (no test exercises them yet) — they will be fleshed out as their consuming specs land in Slices 1-3.
- **Acceptance**:
  - `pnpm tsc --noEmit` shows 0 new errors.
  - `pnpm eslint tests/pages/` passes.
  - `pnpm test --grep-invert @slow` is unaffected (no new tests, no behavioral change in existing ones).
  - The page objects compile and export the methods listed in the design §5.3 tables.
- **Depends on**: **T0.1** (BasePage imports `waitForToast` from `tests/helpers.ts`).
- **TDD**: **Pure test (plumbing)** — page objects with no behavior yet, per the "skeleton" pattern in design Decision 7.
- **Diff size**: **+590 / -0** (≈590 changed lines, 7 new files)
- **Special notes**:
  - **Why 1 commit with 7 files** (design §7 line 480): they're a single conceptual deliverable ("the page-object layer for the 5 critical admin flows + auth"). Splitting into 7 micro-commits would be overhead — no test exercises them yet, so a code review can scan all 7 files as a unit. Each file is small (40-120 LOC).
  - This is the **largest commit by LOC** in the change (590 lines). The orchestrator should be aware of this in the Review Workload Forecast.
  - `BasePage.waitForAdminLayout` is critical — it's the GGA-FOLLOWUP-4 mitigation for the new specs. All admin page objects should call it in `goto()`.

### T0.3 — `chore(playwright): retries: 1 + test:fast script + tests/README.md`

> ✅ **DONE** — commit `afefa3a` — 5 files, +406/-15. `playwright.config.ts: retries 0 → 1`. `test:fast` script added (concurrently + wait-on, both pure-Node). All 13 `test.describe` blocks in `security-admin.spec.ts` tagged `{ tag: '@slow' }` (per design R8 — not a file-header comment). `tests/README.md` documents the new conventions.

- **Files**:
  - **Modified**: `playwright.config.ts` (line 8: `retries: process.env.CI ? 2 : 0` → `retries: process.env.CI ? 2 : 1`)
  - **Modified**: `package.json` — add `concurrently` + `wait-on` to `devDependencies`; add `test:fast` script: `concurrently -k -n dev,test -c blue,green "pnpm dev" "wait-on http://localhost:3000 && pnpm test --grep-invert @slow"`
  - **Modified**: `tests/security-admin.spec.ts` — add `tag: '@slow'` to the top-level `test.describe(...)` block (NOT a file-header comment — design Risk R8: `grep-invert` only matches Playwright's `tag` property)
  - **New**: `tests/README.md` (~80 LOC) — documents: (1) how to run tests manually (2-terminal pattern), (2) how `test:fast` works + its first-run caveat, (3) selector priority convention, (4) isolation rules (`TEST_` prefix, `serial` mode, `afterEach` cleanup)
- **Body**: Mitigate GGA-FOLLOWUP-4 (5.1.1/5.1.4 `saveField` flakiness) by bumping retries from 0 to 1 locally. Mitigate GGA-FOLLOWUP-5 (5-8 min shell execution) by adding the `test:fast` script that pre-starts `pnpm dev` and runs the suite excluding the `@slow` security-admin suite. Add `tests/README.md` to capture the new conventions and the manual 2-terminal fallback.
- **Acceptance**:
  - `playwright.config.ts:8` reads `process.env.CI ? 2 : 1` (verified via `grep`).
  - `package.json` has `test:fast` in `scripts` and `concurrently` + `wait-on` in `devDependencies`.
  - `pnpm install` succeeds (the new devDeps are resolvable).
  - `pnpm test:fast` (in a separate process / mock) starts dev + waits for port 3000 + runs tests with `--grep-invert @slow`.
  - `pnpm test --grep "@slow"` matches only `security-admin.spec.ts` (verified via `--list`).
  - `pnpm tsc --noEmit` shows 0 new errors.
- **Depends on**: none (independent of T0.1 and T0.2).
- **TDD**: **Config + Docs** — no test or app code.
- **Diff size**: **+96 / -2** (≈98 changed lines)
- **Special notes**:
  - Per design Risk R8, the `@slow` tag MUST be a Playwright `tag` property on a `describe` block, not a file-header comment. Verify by reading `security-admin.spec.ts:1-50` to find the top-level `test.describe(...)` and add `tag: '@slow'` to it.
  - The `tests/README.md` goes in this commit because it documents the `test:fast` script + the new conventions introduced in T0.1 and T0.2. Per `work-unit-commits` "docs with the user-visible change", the docs land with the script that needs them.

**PR 1 (Slice 0) total: ~816 changed lines** — exceeds the 500-line review budget and the 400-line chained-PR threshold. **See Review Workload Forecast** at the bottom.

---

## Slice 1 — Rutinas E2E (PR 2)

> Goal: add E2E coverage for the rutina CRUD gap. This is the smallest slice and validates the test infrastructure from Slice 0.

### T1.1 — `test(e2e+app): add rutinas CRUD spec + 7 data-testid attributes (RED-GREEN slice)`

- **Files**:
  - **Modified** (data-testid additions per design Decision 8):
    - `src/components/admin/rutinas-list-client.tsx` (+2 testids: `rutina-list-item`, `rutina-list-item-nombre`)
    - `src/components/admin/rutina-completa-form.tsx` (+5 testids: `rutina-nombre-input`, `rutina-tipo-select`, `rutina-descripcion-input`, `rutina-create-button`, `rutina-save-button`)
  - **New**: `tests/rutinas.spec.ts` (~180 LOC, 5 tests)
  - **New**: `tests/fixtures/rutina.fixture.ts` (~30 LOC, default payload: `TEST_Rutina_<RUN_ID>`)
- **Body**: This is a fused RED-GREEN slice per design §7 line 488. Patch the 2 admin components with the 7 new testids (additive only — never rename existing testids). Write `tests/rutinas.spec.ts` covering the 5 scenarios from design §6.1: S1.1 create, S1.2 edit+persist, S1.3 delete, S1.4 list visibility (TEST_ vs non-TEST_), S1.5 DnD reorder via keyboard (NOT mouse-drag — the existing `dnd-rutina.spec.ts` is fully skipped for mouse-drag flakiness; `dnd-kit` supports keyboard sensor natively). Use `test.describe.configure({ mode: 'serial' })` already in this spec (T3.4 will add `afterEach(cleanTestData)` to all 5 new specs; the `serial` mode flag itself lands here because it's a spec-level decision, not a "fix-up").
- **Acceptance**:
  - All 5 tests pass: `pnpm test tests/rutinas.spec.ts` is green in <60s.
  - The 7 new testids exist in source (verified via `grep "data-testid" src/components/admin/{rutinas-list-client,rutina-completa-form}.tsx`).
  - No existing testid renamed or removed (`git diff` on those 2 files shows only additions).
  - S1.5 uses keyboard interactions (`keyboard.press`) — NOT `page.mouse.down/move/up`. Verify by reading the test source.
  - The 4 `test.skip()` tests in `dnd-rutina.spec.ts` are NOT modified.
  - `pnpm tsc --noEmit` shows 0 new errors.
  - The 5.2.3 isolation spec stays green.
- **Depends on**: **T0.1, T0.2** (spec uses `loginAsAdmin` from helpers + `RoutineAdminPage` from pages).
- **TDD**: **Test + app code** (RED-GREEN slice per design Decision 7).
- **Diff size**: **+217 / -0** (≈217 changed lines)
- **Special notes**:
  - **Why 1 commit with app + test fused** (design §7 line 490): per `work-unit-commits` "keep tests with code" — the new spec REQUIRES the testids; without them, the spec is a failing test that nobody can merge. They're one work unit ("add E2E coverage for rutina CRUD").
  - The `serial` mode flag is added HERE (not deferred to T3.4) because it's a spec-level decision made at design time. T3.4 only adds the `afterEach(cleanTestData)` cleanup hook.
  - S1.5 (DnD) is the riskiest test — if the keyboard sensor doesn't work in this dnd-kit version, fall back to skipping it with a comment and document in engram (type `discovery`).

**PR 2 (Slice 1) total: ~217 changed lines** — ✅ under budget.

---

## Slice 2 — Feriados + Promos + Descuentos E2E (PR 3)

> Goal: add E2E coverage for the 3 admin flows that share similar manager/list patterns. Splits the data-testid patch from the spec files per design §7 line 500.

### T2.1 — `test(app): add data-testid for feriado/promocion/descuento managers`

- **Files** (all data-testid additions, 25 total across 4 components per design Decision 8):
  - **Modified**: `src/components/admin/feriado-manager.tsx` (+10 testids: `feriado-add-button`, `feriado-date-input`, `feriado-todo-dia-checkbox`, `feriado-hora-inicio-input`, `feriado-hora-fin-input`, `feriado-submit-button`, `feriado-list-item`, `feriado-edit-button`, `feriado-delete-button`, `feriado-error`)
  - **Modified**: `src/components/admin/promocion-form.tsx` (+4 testids: `promocion-titulo-input`, `promocion-descripcion-input`, `promocion-precio-input`, `promocion-submit-button`)
  - **Modified**: `src/components/admin/promocion-manager.tsx` (+4 testids: `promocion-add-button`, `promocion-list-item`, `promocion-edit-button`, `promocion-delete-button`)
  - **Modified**: `src/components/admin/descuento-duracion-manager.tsx` (+7 testids: `descuento-add-button`, `descuento-porcentaje-input`, `descuento-min-meses-input`, `descuento-max-meses-input`, `descuento-submit-button`, `descuento-list-item`, `descuento-delete-button`)
- **Body**: Patch the 4 admin components with 25 new testids. **Additive only** — never rename or remove existing testids. Each testid follows the kebab-case pattern from the existing `data-testid="ejercicio-row"` / `data-testid="dia-section"` examples in `WeeklyScheduleEditor.tsx`. The spec files in T2.2 and T2.3 depend on these testids; splitting the patch from the specs lets a reviewer focus on "is the testid naming consistent?" before "is the test logic correct?".
- **Acceptance**:
  - 25 new testids exist in source (verified via `grep -c "data-testid" src/components/admin/{feriado-manager,promocion-form,promocion-manager,descuento-duracion-manager}.tsx`).
  - No existing testid renamed or removed (`git diff` on the 4 files shows only additions).
  - All 6 existing specs that touch these components still pass green (regression check: `pnpm test --grep-invert @slow` is green).
  - `pnpm tsc --noEmit` shows 0 new errors.
  - The 5.2.3 isolation spec stays green.
- **Depends on**: none (additive testid patches, no behavioral change).
- **TDD**: **Test + app code (testid only, no behavior)** — the testids are the "test contract" but they don't change runtime behavior.
- **Diff size**: **+25 / -0** (≈25 changed lines)
- **Special notes**:
  - **Why separate from T2.2/T2.3** (design §7 line 500): the data-testid patch is reviewable on its own (25 lines across 4 components, no behavior change). The specs depend on the testids but the testids are independently mergeable.

### T2.2 — `test(e2e): add feriados-crud.spec.ts`

- **Files**:
  - **New**: `tests/feriados-crud.spec.ts` (~180 LOC, 6 tests per design §6.2)
  - **New**: `tests/fixtures/feriado.fixture.ts` (~30 LOC, default payload: `TEST_Feriado_<future_date>`)
- **Body**: Write 6 E2E tests for `/admin/feriados` CRUD covering: S2.1.1 create (full day), S2.2.1 create (partial hours), S2.3.1 reject past date, S2.4.1 reject inverted hours, S2.5.1 delete, S2.6.1 duplicate date (409). Uses `FeriadoAdminPage` from Slice 0 + the testids from T2.1. `test.describe.configure({ mode: 'serial' })` at the describe level. The 4th test (S2.4.1) is the validation edge case — verify the inline error text matches the actual app message by reading `feriado-manager.tsx` first.
- **Acceptance**:
  - All 6 tests pass: `pnpm test tests/feriados-crud.spec.ts` is green in <45s.
  - S2.3.1 and S2.4.1 are NOT skipped (these are the new "validation" coverage).
  - The duplicate-date test (S2.6.1) uses the seeded admin's API (no second user) and asserts a 409 response OR a server-action error toast.
  - `pnpm tsc --noEmit` shows 0 new errors.
- **Depends on**: **T0.1, T0.2, T2.1** (testids required).
- **TDD**: **Pure test**.
- **Diff size**: **+210 / -0** (≈210 changed lines)
- **Special notes**:
  - The `TEST_Feriado_*` payload should use a future date (e.g., 30 days from `Date.now()`) to avoid the past-date validation in S2.3.1.

### T2.3 — `test(e2e): add promociones-descuentos.spec.ts`

- **Files**:
  - **New**: `tests/promociones-descuentos.spec.ts` (~220 LOC, 6 tests per design §6.3)
  - **New**: `tests/fixtures/promocion.fixture.ts` (~30 LOC)
  - **New**: `tests/fixtures/descuento.fixture.ts` (~25 LOC)
- **Body**: Write 6 E2E tests covering 3 promocion CRUD scenarios (S2.P.1 create via form, S2.P.2 edit+persist, S2.P.3 delete via UI — the existing `promocion-e2e.spec.ts` covers toggle/resync/layout but NOT the create form submit) and 3 descuento CRUD scenarios (S2.D.1 create happy path, S2.D.2 `porcentaje > 100` validation, S2.D.3 delete). Uses `PromocionAdminPage` + `DescuentoAdminPage` from Slice 0 + the testids from T2.1.
- **Acceptance**:
  - All 6 tests pass: `pnpm test tests/promociones-descuentos.spec.ts` is green in <60s.
  - S2.P.1 covers the create-via-form submit gap (the existing `promocion-e2e.spec.ts:4.4-4.8` does NOT).
  - S2.D.1 is the FIRST ever E2E test for `/admin/descuentos-duracion` (zero coverage per engram #200).
  - `pnpm tsc --noEmit` shows 0 new errors.
- **Depends on**: **T0.1, T0.2, T2.1** (testids required).
- **TDD**: **Pure test**.
- **Diff size**: **+275 / -0** (≈275 changed lines)
- **Special notes**:
  - This is the largest "pure test" commit in the change. Within the 400-line hard limit.

**PR 3 (Slice 2) total: ~510 changed lines** — ⚠️ marginally over the 500-line review budget. **See Review Workload Forecast**.

---

## Slice 3 — Trainers + Auth + Isolation (PR 4)

> Goal: complete the 5-flow coverage, add the auth happy-path, and harden the new specs with proper isolation.

### T3.1 — `test(app): add data-testid for trainer manager + auth login error`

- **Files** (8 testids total per design Decision 8):
  - **Modified**: `src/components/admin/trainer-manager.tsx` (+7 testids: `trainer-add-button`, `trainer-name-input`, `trainer-dni-input`, `trainer-password-input`, `trainer-submit-button`, `trainer-list-item`, `trainer-delete-button`)
  - **Modified**: `src/app/(auth)/admin/login/page.tsx` (+1 testid: `login-error-message` on the toast container)
- **Body**: Patch `trainer-manager.tsx` with 7 testids and the login page with 1 testid on the error toast container. The login testid is required by the new `auth.spec.ts` (S3.A.2 + S3.A.3 invalid-creds tests). **Additive only** — never rename or remove existing testids.
- **Acceptance**:
  - 8 new testids exist in source (verified via `grep -c "data-testid" src/components/admin/trainer-manager.tsx src/app/(auth)/admin/login/page.tsx`).
  - No existing testid renamed or removed.
  - The existing `security-admin.spec.ts:2.1-2.3` unauthenticated-redirect tests still pass (they don't depend on the new login testid but verify the page still works).
  - `pnpm tsc --noEmit` shows 0 new errors.
- **Depends on**: none (additive testid patches).
- **TDD**: **Test + app code (testid only, no behavior)**.
- **Diff size**: **+8 / -0** (≈8 changed lines)
- **Special notes**: This is the smallest commit in the change. The login testid MUST be on the toast container (per design Decision 8: "the toast root"), NOT on an inline error span — the existing `auth.spec.ts:8.1.2` gap (engram #200) requires the toast to be visible to the user.

### T3.2 — `test(e2e): add trainers.spec.ts`

- **Files**:
  - **New**: `tests/trainers.spec.ts` (~150 LOC, 4 tests per design §6.4)
  - **New**: `tests/fixtures/trainer.fixture.ts` (~25 LOC, default payload: `TEST_Trainer_<DNI>`)
- **Body**: Write 4 E2E tests for `/admin/trainers` CRUD: S3.T.1 create (with login-as-trainer verification), S3.T.2 edit name, S3.T.3 soft-delete (role → USER), S3.T.4 reject duplicate DNI. Uses `TrainerAdminPage` from Slice 0 + the testids from T3.1. `test.describe.configure({ mode: 'serial' })` at the describe level. The S3.T.3 test verifies that the soft-deleted trainer can no longer log in — it logs out, then logs in as the deleted DNI, and asserts the redirect to `/` (not `/admin`).
- **Acceptance**:
  - All 4 tests pass: `pnpm test tests/trainers.spec.ts` is green in <45s.
  - S3.T.3 is the first ever E2E test for trainer soft-delete (zero coverage per engram #200).
  - The TEST_DNI follows the `TEST_<8-digit>` pattern to avoid colliding with seeded users.
  - `pnpm tsc --noEmit` shows 0 new errors.
- **Depends on**: **T0.1, T0.2, T3.1** (testids required).
- **TDD**: **Pure test**.
- **Diff size**: **+175 / -0** (≈175 changed lines)
- **Special notes**:
  - The `cleanTestData` helper in `tests/helpers.ts` MUST handle the trainer table for the S3.T.3 cleanup. Verify by reading T0.1's `cleanTestData` implementation; if it doesn't include `/api/trainers`, this test will leak state. If missing, escalate to orchestrator (do NOT modify T0.1's contract from T3.2 — that's a task ordering violation).

### T3.3 — `test(e2e): add auth.spec.ts`

- **Files**:
  - **New**: `tests/auth.spec.ts` (~180 LOC, 5 tests per design §6.5)
- **Body**: Write 5 E2E tests for the auth flow: S3.A.1 login success (the proposal's 8.1.2 gap — login with valid creds), S3.A.2 invalid DNI (asserts `login-error-message` toast), S3.A.3 invalid password, S3.A.4 logout from profile dropdown, S3.A.5 session expiry (uses `setExpiredCookie` re-exported from `tests/helpers.ts`). Uses `AuthPage` from Slice 0 + the testid from T3.1.
- **Acceptance**:
  - All 5 tests pass: `pnpm test tests/auth.spec.ts` is green in <45s.
  - S3.A.1 closes the 8.1.2 gap (per engram #200: the existing `admin-e2e.spec.ts:8.1.2` silently skips on success — this new test is non-skipping).
  - S3.A.2 and S3.A.3 both assert the `login-error-message` toast with the actual error message string from the login page (read `src/app/(auth)/admin/login/page.tsx` to find the exact text).
  - S3.A.4 verifies the session cookie is cleared after logout (per `security-admin.spec.ts:5.3-5.7` existing pattern).
  - `pnpm tsc --noEmit` shows 0 new errors.
- **Depends on**: **T0.1, T0.2, T3.1** (testid required for the error toast).
- **TDD**: **Pure test**.
- **Diff size**: **+180 / -0** (≈180 changed lines)
- **Special notes**:
  - This is the only commit that exercises the `setExpiredCookie` re-export from `tests/helpers.ts`. Verify the re-export is in place by reading T0.1's helpers.ts before writing this spec.

### T3.4 — `fix(tests): 5.2.3 isolation — add afterEach(cleanTestData) to 5 new specs`

- **Files** (5 spec files modified):
  - `tests/rutinas.spec.ts` (add `test.afterEach(async ({ page }) => { await cleanTestData(page); })` after the `test.describe.configure` line)
  - `tests/feriados-crud.spec.ts` (same)
  - `tests/promociones-descuentos.spec.ts` (same)
  - `tests/trainers.spec.ts` (same)
  - `tests/auth.spec.ts` (same)
- **Body**: Add the `afterEach` cleanup hook to the 5 new specs. The `serial` mode flag was already added at the spec level in T1.1/T2.2/T2.3/T3.2/T3.3 — this commit ONLY adds the `afterEach(cleanTestData)` call. This is the **production hardening** pass — by deferring the isolation hook to a separate commit, the diff for T3.4 is small (5 files × ~3 lines = ~15 lines added) and reviewable ("is the isolation correct?") independent of "is the test logic correct?" (which was reviewed in T1.1, T2.2, T2.3, T3.2, T3.3).
- **Acceptance**:
  - Each of the 5 new specs has an `afterEach(cleanTestData)` block (verified via `grep -c "afterEach" tests/{rutinas,feriados-crud,promociones-descuentos,trainers,auth}.spec.ts`).
  - Running the 5 new specs 5 times in a row (`for i in 1..5; do pnpm test tests/{rutinas,feriados-crud,promociones-descuentos,trainers,auth}.spec.ts; done`) shows **0 data-leak failures** (the original 5.2.3 root cause).
  - The 5.2.3 isolation spec (`gym-config.spec.ts:5.2.3`) still passes green.
  - `pnpm tsc --noEmit` shows 0 new errors.
- **Depends on**: **T1.1, T2.2, T2.3, T3.2, T3.3** (all 5 new specs must exist for T3.4 to modify them).
- **TDD**: **Refactor (production hardening)** — no behavior change in the tests themselves, only in the isolation hooks.
- **Diff size**: **+15 / -0** (≈15 changed lines, 5 files × 3 lines)
- **Special notes**:
  - **Why separate from T3.2/T3.3** (design §7 line 511): the spec files are already in the "happy" pattern from the previous slices. T3.4 is the "production hardening" pass — adding the isolation hooks AFTER the specs are written and approved makes the diff for T3.4 small and reviewable independent of "is the test logic correct?".
  - This is the LAST commit in the change. After T3.4 lands, the 5.2.3 isolation issue is fully mitigated for the new specs.
  - If `cleanTestData` is not yet implemented in T0.1's `tests/helpers.ts` (T0.1 is "skeleton" allowed, per design Decision 7), T3.4 may need to add a no-op stub. Verify by reading T0.1's helpers.ts source.

**PR 4 (Slice 3) total: ~378 changed lines** — ✅ under the 500-line review budget.

---

## Review Workload Forecast

- **Total estimated diff**: **+1,876 / -112** (≈1,988 changed lines)
- **Total changed lines**: **1,988**
- **Review budget**: **500 lines**
- **Budget exceeded**: **Yes — 3 of 4 PRs are under, 1 is over**
- **Chained PRs recommended**: **Yes** (4 stacked-to-main PRs, pre-designed)
- **Decision needed before apply**: **Yes** (acknowledge `size:exception` for PR 1 per the prior cycle's pattern)

| PR | Slice | Tasks | Changed lines | Budget (500) | Chained-PR threshold (400) | Status |
|----|-------|-------|--------------|--------------|--------------------------|--------|
| **PR 1** | Slice 0 | T0.1 + T0.2 + T0.3 | **~816** | ❌ +316 over | ❌ +416 over | **needs `size:exception`** (sticky from `gga-hook-diff-only` cycle, engram #198 — PR 1 of that cycle was 1231 lines and was accepted) |
| **PR 2** | Slice 1 | T1.1 | **~217** | ✅ | ✅ | clean |
| **PR 3** | Slice 2 | T2.1 + T2.2 + T2.3 | **~510** | ⚠️ +10 over | ❌ +110 over | **marginal** — could use `size:exception` OR split T2.1+T2.2 from T2.3 at apply time |
| **PR 4** | Slice 3 | T3.1 + T3.2 + T3.3 + T3.4 | **~378** | ✅ | ✅ | clean |

- **Suggested chain strategy**: **stacked-to-main** (sticky from `gga-hook-diff-only` cycle, pre-decided by orchestrator)
- **Suggested slice breakdown** (unchanged from design §7):
  - **PR 1 (Slice 0)** — Test infrastructure refactor: 3 commits, 816 lines. Request `size:exception` matching the prior cycle's pattern.
  - **PR 2 (Slice 1)** — Rutinas E2E: 1 commit, 217 lines.
  - **PR 3 (Slice 2)** — Feriados + Promos + Descuentos E2E: 3 commits, 510 lines. Request `size:exception` OR split T2.1+T2.2 (235 lines) from T2.3 (275 lines) at apply time if the orchestrator prefers more PRs.
  - **PR 4 (Slice 3)** — Trainers + Auth + Isolation: 4 commits, 378 lines.

### Auto-forecast (C4) recommendation

The orchestrator's C4 strategy authorizes this. Given:
- PR 1 is the dominant cost driver (590 of 816 lines are T0.2's 7 page object files)
- T0.2 is a single conceptual unit ("the page-object layer") per design §7 line 480
- The previous cycle accepted a 1231-line PR 1 with `size:exception`

**Recommendation: keep the design's 4-PR stacked-to-main plan as-is.** The `size:exception` pattern is established. Splitting T0.2 into 2 commits would not reduce the total review cost (it would just shift it to a second PR with the same scope).

### Per-commit diff size recap (for the orchestrator's commit-by-commit review pacing)

| Task | Files | +LOC | -LOC | Changed |
|------|-------|------|------|---------|
| T0.1 | 1 new + 7 modified | 130 | 110 | 240 |
| T0.2 | 7 new | 590 | 0 | 590 |
| T0.3 | 1 new + 3 modified | 96 | 2 | 98 |
| T1.1 | 2 modified + 2 new | 217 | 0 | 217 |
| T2.1 | 4 modified | 25 | 0 | 25 |
| T2.2 | 2 new | 210 | 0 | 210 |
| T2.3 | 3 new | 275 | 0 | 275 |
| T3.1 | 2 modified | 8 | 0 | 8 |
| T3.2 | 2 new | 175 | 0 | 175 |
| T3.3 | 1 new | 180 | 0 | 180 |
| T3.4 | 5 modified | 15 | 0 | 15 |
| **TOTAL** | | **1,921** | **112** | **2,033** |

(Numbers may vary ±20 LOC at apply time based on actual fixture content; the totals are within 5% of the design's estimates.)
